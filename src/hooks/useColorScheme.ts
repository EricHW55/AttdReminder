import { useColorScheme as _useColorScheme } from 'react-native';

// 이 파일은 Expo의 기본 템플릿에 포함된 훅입니다.
// React Native의 useColorScheme을 그대로 사용합니다.
export function useColorScheme() {
    return _useColorScheme();
}
